<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiAppHotel.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:sf="clr-namespace:Syncfusion.Maui.Toolkit.SegmentedControl;assembly=Syncfusion.Maui.Toolkit"
    xmlns:pages="clr-namespace:MauiAppHotel.Pages"
    xmlns:views="clr-namespace:MauiAppHotel.Views" 
    Shell.FlyoutBehavior="Flyout"
    Title="MauiAppHotel">

    <FlyoutItem Title="Hospedagem" Icon="home.png">
        <ShellContent 
            ContentTemplate="{DataTemplate views:ContratacaoHospedagem}" 
            Route="ContratacaoHospedagem" />
    </FlyoutItem>

    <FlyoutItem Title="Sobre">
        <Shell.ItemTemplate>
            <DataTemplate>
                <Grid ColumnDefinitions="45, *" Padding="10" BackgroundColor="Transparent">
                    <Label Text="ℹ" 
                           FontSize="20" 
                           TextColor="Red" 
                           VerticalOptions="Center" 
                           HorizontalOptions="Center" />

                    <Label Grid.Column="1" 
                           Text="Sobre" 
                           TextColor="Red" 
                           FontAttributes="Bold" 
                           VerticalOptions="Center" 
                           FontSize="16" />
                </Grid>
            </DataTemplate>
        </Shell.ItemTemplate>

        <ShellContent 
            ContentTemplate="{DataTemplate views:Sobre}" 
            Route="Sobre" />
    </FlyoutItem>

    <ShellContent
        Title="Dashboard"
        Icon="{StaticResource IconDashboard}"
        ContentTemplate="{DataTemplate pages:MainPage}"
        Route="main" />

    <ShellContent
        Title="Projects"
        Icon="{StaticResource IconProjects}"
        ContentTemplate="{DataTemplate pages:ProjectListPage}"
        Route="projects" />

    <ShellContent
        Title="Manage Meta"
        Icon="{StaticResource IconMeta}"
        ContentTemplate="{DataTemplate pages:ManageMetaPage}"
        Route="manage" />

    <Shell.FlyoutFooter>
        <Grid Padding="15">
            <sf:SfSegmentedControl x:Name="ThemeSegmentedControl" 
                VerticalOptions="Center" HorizontalOptions="Center" SelectionChanged="SfSegmentedControl_SelectionChanged"
                SegmentWidth="40" SegmentHeight="40">
                <sf:SfSegmentedControl.ItemsSource>
                    <x:Array Type="{x:Type sf:SfSegmentItem}">
                        <sf:SfSegmentItem ImageSource="{StaticResource IconLight}" SemanticProperties.Description="Light mode"/>
                        <sf:SfSegmentItem ImageSource="{StaticResource IconDark}" SemanticProperties.Description="Dark mode"/>
                    </x:Array>
                </sf:SfSegmentedControl.ItemsSource>
            </sf:SfSegmentedControl>
        </Grid>
    </Shell.FlyoutFooter>

</Shell>